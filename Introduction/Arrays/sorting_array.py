"""
with sorting, we put elements in ordered sequence 
here we make use of the sort() function
"""

import numpy as np

arr = np.array([[1,3,4,5,2,6], [2,4,5,62,5,7]])
arr1 = np.array(['Owen', 'Lynnate', 'Bill'])
print(np.sort(arr))
print(np.sort(arr1))
